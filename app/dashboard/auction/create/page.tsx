import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Auction",
  description: "Create a new guild auction for rare items, mounts, and dungeon loot.",
};

export default function page() {
  return (
    <div>
      
    </div>
  )
}